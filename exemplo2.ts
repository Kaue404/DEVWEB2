namespace exemplo2 {
    //VAR, LET, CONST
    let numero: number;
    numero = 5;

    let resultado: number;
    resultado = numero + numero;
    console.log("A soma entre " + numero + " Com " + numero + " é igual a: " + resultado);

    resultado = numero - 4;
    console.log(`A subtração entre ${numero} com 4 é igual a: ${resultado}`);

    resultado = numero * 3;
    console.log(`A multiplicação entre ${numero} vezes 3 é igual a: ${resultado}`);

    resultado = numero / 2;
    console.log(`A divisão do ${numero} por 2 é igual a: ${resultado}`);
}